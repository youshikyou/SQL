SELECT FROM /  NULL / ORDER BY / MAX / MIN / SUM / AVG / GROUP BY / HAVING / WHERE / LIKE / * % / IN / OR / AND / COUNT
CASE WHEN THEN ELSE END AS
WITH table AS
JOIN ON /LEFT JOIN ON
LEFT /  RIGHT / LENGTH / STRPOS(text,arg) / POSITION(arg IN text) / PARTITION BY
RANK()/DENSE_RANK()
OVER
DATE_TRUNC
DATE_PART
NTILE

